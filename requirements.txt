# Convenience file only. pyproject.toml is the authoritative dependency source;
# CI installs `pip install -e ".[test]"`. Keep this in sync when either changes.

# Runtime
numpy>=1.21
numba>=0.56
scikit-learn>=1.6
scipy>=1.7

# Testing
pytest>=7.0
pytest-cov>=4.0
pandas>=1.3
skrebate>=0.6
